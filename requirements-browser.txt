# VarPredBrowser - Browser-Only Dependencies
# Use this if you only need to run the browser server (no Hail processing)

# Core data processing
polars>=0.20.0
numpy>=1.24.0,<2.0.0  # pyBigWig requires NumPy 1.x

# Configuration
pyyaml>=6.0

# Browser Server (FastAPI)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Data formats
pyarrow>=14.0.1
pybigwig>=0.3.22

# HTTP client (for fetching AlphaFold structures)
requests>=2.32.3
